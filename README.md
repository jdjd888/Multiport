```
apt update && apt install -y bzip2 gzip coreutils screen curl wget tcpdump dsniff grepcidr dnsutils -y && wget -P /root -N --no-check-certificate https://raw.githubusercontent.com/SandakanVPNTrickster/Multiport/main/setup.sh && chmod +x setup.sh && "/root/setup.sh"
```
